#!/usr/bin/env node
const app = require('../app');
const port = process.env.PORT || 4040;

app.set('port', port);

app.listen(port, () => {
  console.log('Hello AllTheRooms team!! http://localhost:' + port);
});